# TODO:
# This function should allow users to:
# - return the specification of the expected argument
# - return the inner specification which failed (for recursive specifications)
# - return the inner specification at a printed index
